# 🚀 Vercel Pro Deployment - Converto Business OS
name: Deploy to Vercel Pro

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

jobs:
  deploy:
    name: Deploy to Vercel Pro
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build project
        working-directory: ./frontend
        run: npm run build

      - name: Deploy to Vercel Pro
        uses: amondnet/vercel-action@v25
        id: vercel-deploy
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
          working-directory: ./frontend

      - name: Health Check
        run: |
          sleep 10
          DEPLOYMENT_URL="${{ steps.vercel-deploy.outputs.preview-url }}"
          echo "Checking health at: $DEPLOYMENT_URL/api/health"
          curl -f "$DEPLOYMENT_URL/api/health" || exit 1

      - name: Deployment Summary
        run: |
          echo "🚀 Deployment successful!"
          echo "📍 URL: ${{ steps.vercel-deploy.outputs.preview-url }}"
          echo "📊 Logs: vercel logs ${{ steps.vercel-deploy.outputs.preview-url }} --follow"
